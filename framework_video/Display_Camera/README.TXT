
The imaging software is in a separate partition on the uSD card that is 
mounted as /data

The programs are:

    displayCamera		A test all openCV.  This does _NOT_ work!

    displayCamera_V4L2		Use V4L to capture image data, display with openCV.

    findColor			Find a green object.


